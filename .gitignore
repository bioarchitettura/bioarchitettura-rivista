# This .gitignore is appropriate for repositories deployed to Netlify
# using Jekyll with a Gemfile

# Basic Jekyll gitignores (synchronize to Jekyll.gitignore)
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Additional Ruby/bundler ignore for when you run: bundle install
/vendor

# For Netlify deployment, include Gemfile.lock for deployment stability
# Unlike GitHub Pages, Netlify uses the exact versions specified in Gemfile.lock
